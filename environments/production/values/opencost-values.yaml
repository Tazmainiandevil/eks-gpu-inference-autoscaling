# Production environment - OpenCost with prod cluster settings and higher resource limits
clusterID: eks-gpu-cluster-prod
replicaCount: 2

resources:
  limits:
    memory: 1Gi
  requests:
    cpu: 200m
    memory: 512Mi
