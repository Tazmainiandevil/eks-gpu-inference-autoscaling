# Terraform
.terraform/
.terraform*
terraform.tfstate*
terraform.tfstate.*.backup
.terraform.lock.hcl
terraform/backend.hcl          # local backend config — contains bucket/key, never commit
# *.tfvars are committed (architecture config, no secrets).
# Put personal overrides (real email, etc.) in *.local.tfvars — these are gitignored:
terraform/environments/*.local.tfvars

# AWS
.aws/
credentials
config
*.pem
*.key

# Helm
charts/
Chart.lock
*.tgz

# Kustomize
**/generated/
**/patched/

# Kubernetes / EKS
kubeconfig
*.kubeconfig
k8s/generated/
k8s/patched/

# ArgoCD / GitOps
argocd-appset-generated/
argocd/generated/

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Logs and Temp Files
*.log
*.tmp
*.bak
*.old
scaling-results-*.json         # generated by scripts/validate-scaling.sh
